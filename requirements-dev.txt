-r requirements.txt

pytest>=8.0.0             # test suite
py-solc-x>=2.0.2          # compile + deploy the Solidity contract
web3[tester]>=6.11.0      # in-process EVM for contract integration tests
